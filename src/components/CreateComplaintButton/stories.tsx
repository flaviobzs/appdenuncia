import { Story, Meta } from '@storybook/react/types-6-0'
import CreateComplaintButton from '.'

export default {
  title: 'CreateComplaintButton',
  component: CreateComplaintButton
} as Meta

export const Default: Story = () => <CreateComplaintButton />
