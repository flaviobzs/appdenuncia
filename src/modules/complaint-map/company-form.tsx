import React, { useEffect } from 'react'

export default function CompanyForm() {
  // const company = useCompany();

  // const formMethods = useForm({

  // });

  // useEffect(() => {
  //   if (isSuccess) {
  //     // TODO next page
  //     alert('SAVE SUCCESS');
  //   }
  // }, [isSuccess, isError]);

  const onSubmit = (data) => {
    // event.target.preventDefault();
    // const formattedData = formatFormDataToRemote(data);
    // company.mutate(formattedData);
  }

  return (
    <form>
      {/* ACTIVITY FIELD */}

      {/* LOADER */}
      {/* {isLoading && <Loader color='#82288d' />} */}
    </form>
  )
}
